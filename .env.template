SUPABASE_DB_PASSWORD=XXXXXXXXXX
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_your-key
RESEND_API_KEY=XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
#Sandbox sender. A verified domain is required before sending to real recipients.
RESEND_FROM_EMAIL=OpenDayCare <onboarding@resend.dev>
#Absolute origin used to build the activation link. Origin only: no credentials, path, query or fragment.
APP_URL=http://localhost:3000
#32 random bytes as base64url without padding (43 characters of [A-Za-z0-9_-]).
#Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
#Rotating this value revokes every pending invitation.
INVITATION_HASH_SECRET=XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
#This key has the ability to bypass Row Level Security. Never share it publicly. If leaked, generate a new JWT secret immediately. Prefer using Secret API keys instead.
SUPABASE_SERVICE_ROLE_KEY=XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
